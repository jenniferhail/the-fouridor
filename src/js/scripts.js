document.addEventListener("DOMContentLoaded", function() {

    'use strict';

    // code
	mainContent.init();
    cookieNotice.init();
    notification.init();
	mobileMenu.init();
	slider.init();
	ajax.init();
	// accordions.init();
    // rellax.init();
    // aos.init();

});
